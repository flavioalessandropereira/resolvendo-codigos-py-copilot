# Agora vamos solicitar uma string e um número inteiro como entrada. Depois iremos que retornar a string repetida o número de vezes informado
string = input("Digite uma palavra: ")  
numero = int(input("Quantas vezes você quer repetir essa palavra? "))

print((string + " ") * numero)